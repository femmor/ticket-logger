import Spinner from '../components/Spinner';
import { BackButton } from '../components';

const Tickets = () => {
  return (
    <div>
      <BackButton url="/" />
      <h1>Tickets</h1>
    </div>
  );
};
export default Tickets;
